@model LeaveApplicationApp.Models.LeaveApplication

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>LeaveApplication</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="WorkLeaveType" class="control-label">Leave Type</label>
                <select asp-for="WorkLeaveType" class="form-control" asp-items="ViewBag.LeaveType">
                    <option value="">--Select a Leave Type--</option>
                </select>
                
                <span asp-validation-for="WorkLeaveType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="StartDate" class="control-label">Start Date</label>
                <input asp-for="StartDate" class="form-control" />
                <span asp-validation-for="StartDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EndDate" class="control-label">End Date</label>
                <input asp-for="EndDate" class="form-control" />
                <span asp-validation-for="EndDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="FkEmployeeId" class="control-label">Employee</label>
                <select asp-for="FkEmployeeId" class ="form-control" asp-items="ViewBag.FkEmployeeId">
                    <option value="">--Select an Employee--</option>
                </select>
                
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
